-r requirements.txt
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.27.2
fakeredis==2.25.0
freezegun==1.5.1
# httpx mock for the geoip refresh tests — lets us simulate MaxMind's
# tarball download + 304 + 401 paths without a real network round-trip.
respx==0.21.1
